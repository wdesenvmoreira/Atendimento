import Controller from './Controller'

// criando o usuário dinamicamente
export default class UsuarioController extends Controller {
  // eslint-disable-next-line no-useless-constructor
  constructor (Usuario) {
    super(Usuario)
  }
}